PORT=3001
# Optional: SSL cert paths for HTTPS/WSS (leave unset for dev)
# SSL_KEY=/path/to/key.pem
# SSL_CERT=/path/to/cert.pem
# On Mac, PostgreSQL uses your system username (not "postgres"). Examples:
# No password: postgresql://YOUR_MAC_USERNAME@localhost:5432/desinetwork
# With password: postgresql://YOUR_MAC_USERNAME:password@localhost:5432/desinetwork
DATABASE_URL=postgresql://praneethbonagiri@localhost:5432/desinetwork
# Supabase Storage for images (production). Get from Supabase → Settings → API
# URL: https://YOUR_PROJECT_REF.supabase.co
# SERVICE_KEY: service_role key (never expose publicly)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Email verification (one-time passcode via Resend)
# Get API key: https://resend.com/api-keys
# Without RESEND_API_KEY, the OTP is logged to the server console only.
RESEND_API_KEY=
# Optional: use after verifying your domain in Resend (default: onboarding@resend.dev)
EMAIL_FROM="DesiNetwork <verify@otp.desinetwork.co>"
# In dev, send all verification emails to Resend test inbox: delivered@resend.dev (view at https://resend.com/emails)
# EMAIL_DEV_TO=delivered@resend.dev
# APP_NAME=DesiNetwork
# Set LOG_OTP_TO_CONSOLE=true to always print the verification code in the server terminal (for testing)
# LOG_OTP_TO_CONSOLE=true
